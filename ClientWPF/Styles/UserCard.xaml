<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                    >
    <Style TargetType="ListViewItem" x:Key="UserCard">
        <Setter Property="Width" Value="auto"/>
        <Setter Property="Height" Value="auto"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Grid Margin="0 2 0 14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <Grid Width="40" Height="40" Panel.ZIndex="1" Margin="15 10 0 10">
                            <Border CornerRadius="50" Background="{DynamicResource BackGround}"/>
                            <iconPacks:PackIconModern Width="20"
                                                      Opacity="0.7"
                              Height="20"
                              Kind="User" Margin="0" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            <Border HorizontalAlignment="Right" Height="10" Width="10" CornerRadius="10" Margin="0 0 3 1" VerticalAlignment="Bottom" Style="{DynamicResource Status}"/>
                        </Grid>
                        <Border x:Name="border" Grid.ColumnSpan="2" CornerRadius="20" Style="{DynamicResource SelectedUserBorder}"/>
                        <Grid Grid.Column="1">
                            <TextBlock Style="{DynamicResource BiggerNormal}" Foreground="{DynamicResource Pallete3}" Text="{Binding Name}" />
                        </Grid>
                        <Button Grid.ColumnSpan="2" Background="Transparent" BorderThickness="0" Command="{Binding DataContext.SelectUserCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"
                               >
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource Pallete1}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>