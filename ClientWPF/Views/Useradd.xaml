<UserControl x:Class="ClientWPF.Views.Useradd"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientWPF.Views"
             xmlns:vm="clr-namespace:ClientWPF.ViewModels"
             mc:Ignorable="d" 
             Style="{StaticResource DefaultPage}"
             Visibility="{Binding ViewVisibility}"
             d:DesignHeight="450" d:DesignWidth="800" Margin="0" Padding="0">
    <d:UserControl.DataContext>
        <vm:UserVM/>
    </d:UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.ColumnSpan="3" Panel.ZIndex="-1">
            <Border Grid.ColumnSpan="2" Background="{StaticResource Pallete2}" CornerRadius="15 15 0 0"/>
        </Grid>
        <Grid Grid.ColumnSpan="3" Grid.Row="1" Grid.RowSpan="3" Panel.ZIndex="-1">
            <Border Grid.ColumnSpan="2" Background="{StaticResource BackGround}" CornerRadius="0 0 15 15"/>
        </Grid>
        <Grid Grid.Row="1" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.ColumnSpan="3" FontWeight="SemiBold" Text="Podaj nazwę użytkownika" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="25"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" Grid.Row="1" Grid.Column="1" Height="30">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter"
                        Command="{Binding AddUser}"/>
                </TextBox.InputBindings>
            </TextBox>
            <Button Style="{StaticResource BasicButt}" Command="{Binding AddUser}" Grid.Column="1" Grid.Row="3" VerticalAlignment="Center" Width="150" Height="30" Content="Dodaj użytkownika"/>
        </Grid>
    </Grid>
</UserControl>
